// Este archivo reemplaza a environment.ts en los builds de producción
// (ver "fileReplacements" en angular.json, configuración "production").
export const environment = {
  production: true,
  apiUrl: 'https://backtotem.aeye.com.ar',
};
